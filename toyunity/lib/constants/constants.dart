import 'package:flutter/material.dart';

// Colors used in this app
const primaryColor = Color.fromRGBO(32, 32, 32, 1);
const secondaryColor = Color.fromRGBO(251, 137, 13, 1);
const bgColor = Color.fromRGBO(255, 255, 255, 1.0);
const bgLightColor = Color.fromRGBO(239, 238, 238, 0.7294117647058823);
const textFieldColor = Color.fromRGBO(245, 245, 245, 1.0);
const textColor = Colors.black;
const lightTextColor = Colors.black26;
const transparent = Colors.transparent;

const grey = Color.fromRGBO(148, 170, 220, 1);
const white = Color.fromRGBO(255, 255, 255, 1.0);
const purple = Color.fromRGBO(165, 80, 179, 1);
const orange = Color.fromRGBO(251, 137, 13, 1);
const green = Color.fromRGBO(43, 169, 119, 1.0);
const red = Colors.red;

// Default App Padding
const appPadding = 16.0;
const webPadding = 90.0;
const kMaxWidth = 1232.0;

const spacerWidth = SizedBox(
  width: appPadding,
);

const spacerHeight = SizedBox(
  height: appPadding,
);
